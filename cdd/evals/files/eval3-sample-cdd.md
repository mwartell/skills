---
date: 2026-04-05
title: python utilities
current_head: abc1234
agent: "test"
session_type:
topics: [python]
---

# Prompt 1: create constants file

Create a file called `constants.py` with a single variable: `APP_NAME = "myapp"`

# Response 1: created constants.py

Created [constants.py](constants.py) with the `APP_NAME` constant. This is a simple module-level variable used for app identification.

```python
APP_NAME = "myapp"
```

# Prompt 2: add version constant

Add a `VERSION = "1.0.0"` constant to `constants.py` alongside the existing `APP_NAME`.
